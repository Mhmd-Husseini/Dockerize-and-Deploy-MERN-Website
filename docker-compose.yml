version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    networks:
      my-network:
        ipv4_address: 137.184.10.2  # Unique IPv4 address for backend
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    networks:
      my-network:
        ipv4_address: 137.184.10.3  # Unique IPv4 address for frontend
    ports:
      - "3000:3000"
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
      - frontend
    networks:
      my-network:
        ipv4_address: 137.184.10.4  # Unique IPv4 address for nginx

networks:
  my-network:
    driver: bridge
    ipam:
      config:
        - subnet: 137.184.10.0/24  # Define the subnet to allocate IPs from
